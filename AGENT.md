# PhotoChoser Agent Notes

This repository uses `AGENTS.md` as the Codex project guide.

All user requirements, project context, quick run commands, RAW handling rules, and verification steps are documented in `AGENTS.md`.

